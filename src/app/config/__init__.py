"""
Configuration management for the multi-agent evaluation system.

This module contains configuration files and utilities for managing
system settings, provider configurations, and prompt templates.

Key configuration files:
- config_chat.json: LLM provider settings, model configurations, and system prompts
- Evaluation settings: Thresholds, weights, and evaluation parameters

The configuration system supports multiple LLM providers and allows for
easy customization of agent behavior through structured prompt templates
and provider-specific settings.
"""
